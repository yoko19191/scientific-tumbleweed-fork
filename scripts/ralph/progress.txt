# Ralph Progress Log
Started: 2026年 4月14日 星期二 14时00分23秒 CST
---

## Codebase Patterns
- Auth pipeline: `get_optional_user_from_request` in `app/gateway/deps.py` decodes JWT cookie → verifies token_version → returns User. Patch `app.gateway.auth.jwt.decode_token` and `app.gateway.deps.get_local_provider` in tests.
- FastAPI dependency injection: use `Depends(get_current_user_id)` in route signatures for strict auth; `Depends(get_optional_user_id)` for optional/legacy paths.
- Test pattern: use `contextmanager` + `TestClient` with `client.cookies.set(...)` (not per-request cookies) to avoid deprecation warnings.
- Pre-existing test failures (not caused by this branch): `test_feishu_parser.py`, `test_threads_router.py`, `test_custom_agent.py`, `test_gateway_services.py`, `test_skills_custom_router.py`, live tests.

## 2026-04-14 - US-001
- Added `get_current_user_id` async FastAPI dependency to `backend/app/gateway/deps.py`
- Dependency decodes `access_token` cookie, verifies `token_version`, returns `str(user.id)`; raises HTTP 401 for all failure cases
- `get_optional_user_id` preserved unchanged for legacy/optional paths
- Files changed: `backend/app/gateway/deps.py`, `backend/tests/test_deps_get_current_user_id.py`
- **Learnings for future iterations:**
  - `decode_token` is a local import inside `get_optional_user_from_request`; patch it at `app.gateway.auth.jwt.decode_token`
  - `get_local_provider` is a module-level function; patch at `app.gateway.deps.get_local_provider`
  - Use `contextmanager` wrapper (not `yield` method) for TestClient helpers
---

## 2026-04-14 - US-002
- Added `backend/app/gateway/user_prefix.py` with canonical user prefix helpers
- `validate_user_id()`: rejects empty, path separators, backslashes, unsafe chars
- `user_threads_namespace()`, `user_thread_owners_namespace()`, `user_memory_namespace()`, `user_extensions_namespace()` for Store namespace tuples
- `validated_user_id_for_path()` for filesystem path usage
- Files changed: `backend/app/gateway/user_prefix.py`, `backend/tests/test_user_prefix.py`
- **Learnings for future iterations:**
  - Store namespaces are tuples; user-scoped format is `("resource_type", "user:{user_id}")`
  - `Paths` methods in `deerflow.config.paths` already accept `user_id` directly — use `validated_user_id_for_path()` before passing
---

## 2026-04-14 - US-003
- Added `backend/app/gateway/thread_ownership.py` with thread ownership service
- `bind_thread_to_user()`: idempotent binding, rejects ownership conflicts (403)
- `require_thread_owner()`: verifies cookie-authenticated user owns thread; raises 401/404/503; never trusts client-supplied user_id
- `get_thread_owner()`: non-raising variant for conditional logic
- Files changed: `backend/app/gateway/thread_ownership.py`, `backend/tests/test_thread_ownership.py`
- **Learnings for future iterations:**
  - `get_current_user_id` is a local import inside `require_thread_owner`; patch at `app.gateway.deps.get_current_user_id`
  - Async tests need `@pytest.mark.anyio` (not `@pytest.mark.asyncio`) — this project uses anyio plugin
  - `_make_request_with_store(store)` pattern: build a `StarletteRequest` with a minimal scope dict and `app.state.store = store`
---

## 2026-04-14 - US-004
- Enforced owner checks on all thread CRUD and state routes in `backend/app/gateway/routers/threads.py`
- `POST /api/threads`: calls `bind_thread_to_user` after creation when user is authenticated (anonymous threads still allowed)
- `GET/PATCH/DELETE /api/threads/{thread_id}`: calls `require_thread_owner` before reading/modifying data
- `GET/POST /api/threads/{thread_id}/state`, `POST /api/threads/{thread_id}/history`: calls `require_thread_owner` before reading checkpoint data
- `POST /api/threads/search`: scoped to user's `user_threads_namespace`; unauthenticated callers get empty list (not 401)
- `GET /api/threads/listByUser`: uses `user_thread_owners_namespace` instead of global `THREAD_OWNERS_NS`
- `POST /api/threads/bindUser`: delegates to `bind_thread_to_user` from `thread_ownership` module
- `_store_get`, `_store_put`, `_store_upsert` helpers updated to accept optional `ns` parameter for user-scoped namespaces
- Files changed: `backend/app/gateway/routers/threads.py`, `backend/tests/test_threads_router_ownership.py`, `backend/tests/test_threads_router.py`
- **Learnings for future iterations:**
  - `require_thread_owner` is imported at module level in threads.py; patch at `app.gateway.routers.threads.require_thread_owner` in tests
  - `get_optional_user_id` is imported at module level; patch at `app.gateway.routers.threads.get_optional_user_id` for search tests
  - Pre-existing tests that call DELETE without auth needed `require_thread_owner` mock added alongside `get_current_user_id` mock
  - Store helpers (`_store_get`, `_store_put`, `_store_upsert`) now accept `ns` kwarg — pass `user_threads_namespace(user_id)` for user-scoped writes
  - `search_threads` no longer does a checkpointer scan — it only reads from the user-scoped Store namespace (simpler and safer)
---

## 2026-04-15 - US-006
- Routed default /api/langgraph/* traffic through authenticated Gateway (owner checks enforced)
- nginx.local.conf: /api/langgraph/ now rewrites to /api/* on Gateway; added /api/langgraph-direct/ for debugging only
- docker-compose-dev.yaml + docker-compose.yaml: LANGGRAPH_UPSTREAM defaults to gateway:8001, LANGGRAPH_REWRITE defaults to /api/
- scripts/serve.sh: removed gateway-mode-only env override; /api/langgraph is now authenticated in both modes
- frontend/.env.example: updated docs to reflect new default authenticated route
- Files changed: docker/nginx/nginx.local.conf, docker/nginx/nginx.conf, docker/docker-compose-dev.yaml, docker/docker-compose.yaml, scripts/serve.sh, frontend/.env.example, backend/tests/test_langgraph_routing_isolation.py
- **Learnings for future iterations:**
  - nginx.local.conf uses named upstreams (http://gateway, http://langgraph); docker nginx.conf uses variables ($langgraph_upstream) with envsubst
  - LANGGRAPH_UPSTREAM + LANGGRAPH_REWRITE env vars control docker routing; nginx.local.conf is hardcoded for local dev
  - The /api/langgraph-compat/ path in nginx.local.conf is now superseded by the default /api/langgraph/ → Gateway routing
  - Regression test reads nginx.local.conf and docker-compose-dev.yaml directly to assert config correctness
---

## 2026-04-16 - US-009
- Made MemoryMiddleware strict per-user: skips queue when no trusted user_id is available (no global fallback)
- user_id is extracted from runtime.context first, then run metadata — never from request body/query params
- Lead agent prompt injection already used user_id correctly via `_get_memory_context(user_id)`
- Files changed: `backend/packages/harness/deerflow/agents/middlewares/memory_middleware.py`, `backend/tests/test_memory_middleware_strict_user.py`
- **Learnings for future iterations:**
  - `get_memory_config` and `get_memory_data` are imported inside `_get_memory_context()` (lazy imports); patch at source module level: `deerflow.config.memory_config.get_memory_config` and `deerflow.agents.memory.updater.get_memory_storage`
  - `test_memory_queue.py::test_process_queue_forwards_reinforcement_flag_to_updater` is a pre-existing failure — uses old `agent_name` field that was replaced by `user_id` in ConversationContext
  - MemoryMiddleware.after_agent() uses `get_config()` (LangGraph config) for thread_id fallback and metadata.user_id fallback; patch at `deerflow.agents.middlewares.memory_middleware.get_config`
---

## 2026-04-16 - US-010
- Added `user_thread_acp_workspace_dir(user_id, thread_id)` helper to `Paths`
- Fixed `ensure_thread_dirs(thread_id, user_id)` to use user-prefixed acp-workspace (was using legacy global `acp_workspace_dir`)
- `resolve_virtual_path` and `delete_thread_dir` already supported `user_id` parameter
- Files changed: `backend/packages/harness/deerflow/config/paths.py`, `backend/tests/test_user_prefixed_thread_paths.py`
- **Learnings for future iterations:**
  - `ensure_thread_dirs` had a bug: used `acp_workspace_dir(thread_id)` (global) even when `user_id` was provided — now fixed to use `user_thread_acp_workspace_dir`
  - All user-scoped thread paths live under `{base_dir}/users/{user_id}/threads/{thread_id}/`
  - `resolve_virtual_path` with `user_id` resolves against `user_thread_dir` (no `user-data/` subdirectory) — different from legacy which uses `sandbox_user_data_dir`
---

## 2026-04-16 - US-011
- Updated `AioSandboxProvider.acquire()` to accept `user_id`; uses `"user_id:thread_id"` as in-process cache key
- Updated `_get_thread_mounts()` and `_get_extra_mounts()` to accept `user_id` and pass through to all host path helpers
- Added `user_id` parameter to `host_acp_workspace_dir()` in `paths.py`
- Updated `_discover_or_create_with_lock()` to use user-scoped thread dir for lock file when `user_id` is present
- Updated `_create_sandbox()` to accept `user_id` and use correct cache key
- Fixed existing test `test_discover_or_create_only_unlocks_when_lock_succeeds` to use new signature
- Files changed: `backend/packages/harness/deerflow/community/aio_sandbox/aio_sandbox_provider.py`, `backend/packages/harness/deerflow/config/paths.py`, `backend/tests/test_sandbox_user_prefixed_paths.py`, `backend/tests/test_aio_sandbox_provider.py`
- **Learnings for future iterations:**
  - `_thread_sandboxes` dict uses `"user_id:thread_id"` as key when both are present; callers of `acquire()` must pass `user_id` from runtime context
  - `_discover_or_create_with_lock` signature changed from `(thread_id, sandbox_id)` to `(thread_id, user_id, cache_key, sandbox_id)`
  - `_create_sandbox` signature changed from `(thread_id, sandbox_id)` to `(thread_id, user_id, sandbox_id)`
---

## 2026-04-16 - US-012
- Updated upload and artifact routers to use `require_thread_owner()` for all endpoints
- POST /api/threads/{thread_id}/uploads checks owner before writing files
- GET /api/threads/{thread_id}/uploads/list checks owner before listing files
- DELETE /api/threads/{thread_id}/uploads/{filename} checks owner before deleting files
- GET /api/threads/{thread_id}/artifacts/{path} checks owner for normal paths and .skill archives
- `sandbox_provider.acquire()` now passes `user_id` so mounts use user-prefixed paths
- All endpoints return 401 when unauthenticated (no valid session cookie)
- Files changed: `backend/app/gateway/routers/uploads.py`, `backend/app/gateway/routers/artifacts.py`, `backend/tests/test_uploads_router.py`, `backend/tests/test_artifacts_router.py`
- **Learnings for future iterations:**
  - Existing tests needed to mock `require_thread_owner` to avoid hitting the Store
  - Added `_patch_require_owner()` helper in tests to simplify mocking
  - Added US-012 tests for 401 responses when unauthenticated
---

## 2026-04-16 - US-013
- Updated custom skill manager helpers to accept user_id parameter
- get_custom_skills_dir(), get_custom_skill_dir(), get_custom_skill_file(), get_skill_history_file() now accept optional user_id
- When user_id provided, paths resolve to {base_dir}/users/{user_id}/skills/custom/
- Updated all custom skill router endpoints to use get_optional_user_id() and pass to manager helpers
- list_custom_skills(), get_custom_skill(), update_custom_skill(), delete_custom_skill() now user-scoped
- get_custom_skill_history() and rollback_custom_skill() use user-scoped history files
- install_skill_from_archive() installs to user-scoped directory when user_id provided
- Two users can now have custom skills with same name without conflicts
- Files changed: backend/packages/harness/deerflow/skills/manager.py, backend/app/gateway/routers/skills.py, backend/tests/test_custom_skills_user_prefixed.py
- **Learnings for future iterations:**
  - Custom skill manager helpers follow same pattern as other user-scoped resources (memory, agents, etc.)
  - get_optional_user_id() extracts user from request.state.auth.user.id (not a function call to mock)
  - User-scoped custom skills path: {base_dir}/users/{user_id}/skills/custom/{skill_name}/
  - History files stored in .history subdirectory: {base_dir}/users/{user_id}/skills/custom/.history/{skill_name}.jsonl
  - All manager helpers accept user_id=None for backward compatibility with global fallback
  - Router tests require proper auth middleware mocking - simpler to test manager layer directly
---

## 2026-04-16 - US-014
- Updated get_skills_prompt_section() to accept user_id parameter
- When user_id provided, loads skills with user-scoped enablement state via load_skills(enabled_only=True, user_id=user_id)
- Updated _apply_prompt_via_builder() and legacy apply_prompt_template() to pass user_id to get_skills_prompt_section
- _load_enabled_skills_sync() now accepts user_id parameter (though not used in current cache implementation)
- User A disabling a skill no longer affects user B's skill visibility
- Agent prompt available_skills only includes skills enabled for the run owner
- Files changed: backend/packages/harness/deerflow/agents/lead_agent/prompt.py, backend/tests/test_skill_enablement_user_prefixed.py
- **Learnings for future iterations:**
  - Skill enablement state is read from user-scoped extensions_config.json when user_id provided
  - load_skills() already supported user_id parameter for reading user-scoped config
  - get_skills_prompt_section() needed to pass user_id through to load_skills()
  - Enabled skills cache is global and not user-scoped (future optimization opportunity)
---

## 2026-04-16 - US-015
- Fixed list_agents endpoint to include soul content by passing include_soul=True to _agent_config_to_response
- Fixed list_custom_agents() to pass user_id parameter to load_agent_config() when iterating agents
- All /api/agents endpoints already use get_optional_user_id() and pass user_id to underlying functions
- All /api/user-profile endpoints already use get_optional_user_id() and resolve_user_md(user_id)
- Lead agent factory already loads agent config and SOUL.md using run owner user_id
- Two users can create agents with same name without conflicts
- Added comprehensive tests for agent and user profile isolation
- Files changed: backend/app/gateway/routers/agents.py, backend/packages/harness/deerflow/config/agents_config.py, backend/tests/test_agents_user_prefixed.py
- **Learnings for future iterations:**
  - Agent and user profile isolation was mostly implemented, just needed bug fixes
  - list_custom_agents() was missing user_id parameter when calling load_agent_config()
  - list_agents endpoint was missing include_soul=True parameter
  - resolve_agent_dir(), resolve_user_md() already support user_id parameter
---
